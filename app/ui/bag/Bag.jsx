import React from 'react';

import { Heading } from '@chakra-ui/react';

const Bag = () => {
	// const { data } = await fetchBagByUserId(6);

	return (
		<div>
			<Heading as={'h2'} mb={'30px'}>
				Bag
			</Heading>
		</div>
	);
};

export default Bag;
